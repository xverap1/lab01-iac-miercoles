name: Saludo

on:
  push:
    branches:
      - main
      - develop

jobs:
 saludo:
   runs-on: ubuntu-latest
   steps:
      - name: Saludo desde main
        run: echo "Hola desde main, soy Xiomara Natali"

      - name: Saludo desde develop
        run: echo "Hola desde develop, soy Xiomara Natali"
